// sting to number
const score='21s'
const numScore=Number(score);
console.log(typeof(score),typeof numScore,numScore);
// '22'=number
// '32wa=null 

// number to sting
const year=2015;
const yearStr=String(year)

// number,string to boolean
// boolean true=1 false=0
const isRain=false;
const test=undefined

const check=Boolean(test)
console.log(check);

console.log(Number(false));


